services:
  httpd:
    image: httpd:latest
    container_name: recipewise
    volumes:
      - /home/misos/Documents/Martin/Infrastructure/recipewise:/usr/local/apache2/htdocs/
    ports:
      - 8123:80
    restart: unless-stopped
