name: recipe_repository
description: Repository for storing and retrieving recipes
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  equatable: ^2.0.5
  recipe_api:
    path: ../../providers/recipe_api

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.7
  very_good_analysis: ^6.0.0
